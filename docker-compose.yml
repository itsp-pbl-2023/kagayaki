version: "3"
services:
  app:
    container_name: frontend
    build:
      context: .
    volumes:
      - ./src:/app
    environment:
      - WATCHPACK_POLLING=true
    ports:
      - 3000:3000
    command: sh -c "npm run dev --turbo"
